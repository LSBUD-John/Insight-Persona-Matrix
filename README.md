# LSBUD Insight Persona Engine, Library and Matrix - restored full functionality

This build restores the broader persona matrix, matrix filters, recommendation preview and library PDF links.

Important: To make a new PDF appear as a download, update `library.json` with `pdfStatus: "PDF complete"` and `pdfFile: "Exact_File_Name.pdf"`. You do not need to change `index.html` if the data file is updated.
