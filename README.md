# LSBUD Insight Matrix-first build

This returns to the stronger Matrix-first build, moves the Engine to a separate tab, and adds the Library tab without reducing matrix functionality.

PDF links are data-driven from `library.json`. Add `pdfStatus: "PDF complete"` and `pdfFile: "Exact_File_Name.pdf"` to link new PDFs.
